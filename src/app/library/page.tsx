import { PlaceholderScreen } from "@/components/shell/PlaceholderScreen";

export default function LibraryPage() {
  return (
    <PlaceholderScreen
      title="Library"
      subtitle="All scored content — reuse ScoreRing, Chip, and Panel from the pattern library."
    />
  );
}
